Article:
    fields:
        title: varchar(50)
        text: text

Category:
    fields:
        title: varchar(50)

Author:
    fields:
        firstName: varChar(64)
        lastName: varchar(64)
        Age: int

