Article:
    fields:
        title: varchar(50)
        text: text

Category:
    fields:
        title: varchar(50)

Author:
    fields:
        fName: varChar(64)
        lName: varchar(64)
        Age: int

