Post:
    fields:
        content: text
        title: varchar(255)

Section:
    fields:
        title: varchar(255)

User:
    fields:
        name: varchar(255)
        email: varchar(255)

Tag:
    fields:
        name: varchar(255)

Category:
    fields:
        title: varchar

Comment:
    fields:
        date: integer
        text: varchar(255)
        author: varchar(255)
